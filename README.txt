Use /ims as the context path.
Open the connection.properties file to specify the database name, username, and password for mysql database server.
Replace the <PUBLIC-IP> in consumerInfo_messages.properties file with stack ip addresses shared. Complete the apiURL for different api to consume.
Open the com.accenture.lkm.web.client package to complete the code for different consumer service class.
Look into the UMLClassDiagram for creating entity and bean. Create as many bean classes required for different prototypes to generate report.
Few JSPs are added to get you started with. Look into the include.jsp, footer.jsp file where different URL are specified to work with in controller.
One sample unit test cases is written to write further unit testing code.